import { NoWhitespaceDirective } from './no-whitespace-validator.directive';

describe('NoWhitespaceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NoWhitespaceDirective();
    expect(directive).toBeTruthy();
  });
});
