import { ShowDirective } from './show-app.directive';

describe('ShowDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDirective(null);
    expect(directive).toBeTruthy();
  });
});
